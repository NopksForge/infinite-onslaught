services:
  redis:
    image: redis
    container_name: redis_dnd
    restart: on-failure
    ports:
      - 6379:6379
    volumes:
      - ./.scripts/redis/config/redis.conf:/redis.conf
    command: redis-server /redis.conf